## Tasks

- [ ] 1. Зафиксировать красный synthetic regression: параллельные `/plan_windows` и `/infer` при worker capacity 4 дают ошибку при текущем Go клиенте; первый worker `503` оставляет поздний повтор того же `payload_id` в `503`. Проверить отсутствие raw text и PII в результатах теста.
- [ ] 2. Провести все Go worker POST через общий, context-aware лимит с bounded wait; отдельно проверить `plan_windows` и `infer`, cancellation/timeout, отсутствие permit leak и двух моделей в полном NER. Один commit, focused `internal/modelclient` tests.
- [ ] 3. Установить bounded HTTP admission/queue перед созданием process claim, связать очередь с общим deadline и ограничением числа/байтов ожидающих; безопасный `429`/`Retry-After` при переполнении. Один commit, synthetic `cmd/pii-service` и `internal/api` concurrency tests, без голодания health/metrics.
- [ ] 4. Исправить `process.Store`/Operation для transient failure: поздний retry того же original повторяет попытку атомарно; concurrent same-ID waiters и конфликтующий payload соблюдают single-writer/409; готовый result/restore стабильны; cancel/partial side effects не публикуют неверный mapping. Один commit, state-machine и HTTP integration tests.
- [ ] 5. Измерить `plan_windows`, RuBERT, GLiNER, Go stages и разрешённый debug I/O, CPU/RAM и full-NER goodput на существующей 4vCPU/8GiB VM без production load injection. Сохранить синтетический профиль и параметры, выбрать одно подтверждённое bottleneck. Один task-scoped документальный commit после approval.
- [ ] 6. На синтетическом профиле, отражающем 1005 ID/448 разных текстов, сравнить baseline и ограниченное Go кэширование только проверенных NER spans по HMAC текста+модель/режим, с in-flight coalescing при безопасной отмене. Если hit rate/полезный goodput заметно растут без роста памяти и потери качества, оставить; иначе не добавлять cache. Один commit для выбранной реализации, tests TTL/limit/ошибка/cancel/ID-specific tokenization.
- [ ] 7. Если профиль показывает значимую цену short-text planning, сделать один worker path с точной tokenizer проверкой и обоими model sources, сохранив long-text contract; иначе, если подтверждён inference bottleneck, сравнить worker inflight 4 vs 8 и минимальную настройку CPU threads/batching готовых моделей без изменения качества. Оставить вариант только при улучшении goodput в прежнем 10s/RAM budget. Не выполнять оба варианта без evidence. Один commit на выбранное изменение, correctness/fail-closed tests.
- [ ] 8. Повторить фиксированный synthetic open-loop short run на той же VM: 983 новых ID за 6s и sustained fresh full-NER load в пределах бюджета, раздельно отчитаться об успехах/отказах, cache hit/miss, p50/p95/p99/max, CPU/RAM, фактической пропускной способности и достижимом envelope. До измерения не отмечать задачу выполненной; отдельный release/deploy/live approval.

Каждый пункт соответствует одному task-scoped commit; пункты 6 и 7 условные. До review и явного approval сервисный код не менять.
